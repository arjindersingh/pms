<div><label class="form-label" for="{{ $name }}">{{ $label }}</label><select class="form-select searchable-select" id="{{ $name }}" name="{{ $name }}" data-placeholder="Search {{ Str::lower($label) }}"><option value="">Select {{ Str::lower($label) }}</option>@foreach($items as $item)<option value="{{ $item->id }}" @selected((string)old($name,$value)===(string)$item->id)>{{ $item->display_name }}</option>@endforeach</select></div>
