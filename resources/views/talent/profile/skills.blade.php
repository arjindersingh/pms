<div class="profile-record-grid">@foreach([['skill','Skills',$profile->skills,$skills],['language','Languages',$profile->languages,$languages]] as [$kind,$title,$selected,$options])<section class="dashboard-card"><h2>{{ $title }}</h2>@foreach($selected as $item)<div class="profile-record"><strong>{{ $item->display_name }}</strong><form method="POST" action="{{ route('talent.profile.remove',[$kind,$item]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-x"></i></button></form></div>@endforeach<form method="POST" action="{{ route('talent.profile.'.$kind) }}" class="mt-4">@csrf @include('talent.profile.master-select',['name'=>$kind.'_id','label'=>ucfirst($kind),'items'=>$options,'value'=>null])<div class="mt-3">@include('talent.profile.master-select',['name'=>'proficiency_level_id','label'=>'Proficiency','items'=>$proficiencyLevels,'value'=>null])</div>@if($kind==='skill')<label class="form-label mt-3">Years of experience</label><input class="form-control" type="number" step=".5" min="0" name="years_experience">@endif @include('talent.profile.switch',['name'=>$kind==='skill'?'is_primary':'is_native','label'=>$kind==='skill'?'Primary skill':'Native language','checked'=>false])<button class="btn btn-portal mt-3">Add {{ $kind }}</button></form></section>@endforeach</div>
